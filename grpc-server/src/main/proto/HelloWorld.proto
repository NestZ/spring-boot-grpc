syntax = "proto3";

package com.nest.grpc;

service HelloService {
  rpc hello(Hello) returns (Greeting);
}

message Hello {
  string firstName = 1;
  string lastName = 2;
}

message Greeting {
  string greeting = 1;
}
